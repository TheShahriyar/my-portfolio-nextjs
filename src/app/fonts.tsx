import { Open_Sans, Kaushan_Script } from "next/font/google";

export const openSans = Open_Sans({
  subsets: ["latin"],
  variable: "--openSans",
});

export const kaushan = Kaushan_Script({
  weight: "400",
  subsets: ["latin"],
  variable: "--kausan",
});
